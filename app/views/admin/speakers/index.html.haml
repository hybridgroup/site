.module
  =render :partial => 'shared/corners'
  .module-inner
    %h1 Administration
=render :partial => 'admin/navigation'
.module
  =render :partial => 'shared/corners'
  .module-inner
    %h2 Speakers

.module
  =render :partial => 'shared/corners'
  .module-inner
    =link_to 'Add Speaker', new_admin_speaker_path
    %table.admin
      %tr
        %th
          id
        %th
          profile id
        %th
          Name
        %th
          Bio
        %th
          Actions
      =render :partial => 'speaker', :collection => @speakers